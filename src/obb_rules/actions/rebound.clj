(ns ^{:added "1.7"
      :author "Pedro Santos"}
  obb-rules.actions.rebound
  "Processes rebound logic"
  (:require [obb-rules.element :as element]))

(defn process
  "Processes the rebound for the given data"
  [config {attacker :attacker board :board info :info :as args}]
  [board info])

